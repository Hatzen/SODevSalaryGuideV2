# https://www.baeldung.com/spring-cloud-bootstrap-properties#quick-example

# Will not be considered for right application context
# https://docs.spring.io/spring-cloud-config/reference/client.html
spring:
  config:
    # import: optional:"http://localhost:8888", "http://config-server:8888", "${sodev.config.url:http://user:password@localhost:8888}"
    # import: optional:configserver:"http://localhost:8888", "http://config-server:8888", "${sodev.config.url:http://user:password@localhost:8888}"
    import: configserver:http://localhost:8888
  cloud:
    config:
      enabled: true
      uri: "http://localhost:8888", "http://config-server:8888", "${sodev.config.url:http://user:password@localhost:8888}"